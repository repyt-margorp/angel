 *
 *		The detection of overestimation requires (t+1)
 *	bytes x_n, since there are cases A = 0x0100...00 and
 *	x_n = 0x0100...00, which is very bad. If x_n can hold
 *	just t bytes, x_n can afford to represent only up to
 *	0x00ff...ff, and this does not overestimate which means
 *	the algorithm does not stop properly. Therefore, the t
 *	bytes input 'to' does not fit for this purpose. One
 *	possibility is that we place temporary 'to' at the buffer
 *	from the address t. 'to' have only t bytes capacity, and
